Description: Do not try to locate idl2wrs_be.py and idl2wrs_gen.py in
 non-standard places.
Author: Frederic Peters <fpeters@debian.org>

--- a/tools/dissector_generators/idl2wrs
+++ b/tools/dissector_generators/idl2wrs
@@ -65,41 +65,8 @@
     exit 1;
 fi
 
-#
-# Run wireshark backend, looking for idl2wrs_be.py and idl2wrs_gen.py
-# in pythons's "site-packages" directory. If cannot find that, then
-# try looking in current directory. If still cannot, then exit with
-# error.
-
-if [ -f $PYTHONPATH/site-packages/idl2wrs_be.py ] && [ -f $PYTHONPATH/site-packages/idl2wrs_gen.py ]; then
-    exec omniidl  -p $PYTHONPATH/site-packages -b idl2wrs_be $@
-    /* not reached */
-fi
-
-# Try current directory.
-
-if [ -f ./idl2wrs_be.py ] && [ -f ./idl2wrs_gen.py ]; then
-    exec omniidl  -p ./ -b idl2wrs_be $@
-    /* not reached */
-fi
-
-# Could not find both idl2wrs_be.py AND idl2wrs_gen.py
-# So let's just try to run it without -p, hoping that the installation
-# set up a valid path.
-
 exec omniidl -b idl2wrs_be $@
 
-old code: not reached
-
-echo "idl2wrs Error: Could not find both idl2wrs_be.py AND idl2wrs_gen.py."
-echo "Please ensure you have the PYTHONPATH variable set, or that idl2wrs_be.py "
-echo "and idl2wrs_gen.py exist in the current directory. "
-echo
-echo "On this system, PYTHONPATH is : $PYTHONPATH"
-echo
-
-exit 2
-
 
 #
 # Editor modelines  -  https://www.wireshark.org/tools/modelines.html
