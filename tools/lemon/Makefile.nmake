
include ..\..\config.nmake

CFLAGS=$(LOCAL_CFLAGS)

{$S}.c{$O}.obj::
   $(CC) $(CFLAGS) -Fd$O\ -c $<

all : lemon.exe

OBJECTS = lemon.obj

lemon.exe	: $(OBJECTS)

clean:
	rm -f $(OBJECTS) lemon.exe
